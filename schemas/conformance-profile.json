{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "conformance-profile.json",
  "title": "ConformanceProfile",
  "description": "Input to conformance/run.py: how to invoke a CLI and which probes to run against it.",
  "type": "object",
  "required": ["schema_version", "tool", "command", "timeout_seconds", "probes"],
  "additionalProperties": false,
  "properties": {
    "schema_version": {
      "type": "string",
      "enum": ["1.0"],
      "description": "Version of this profile format."
    },
    "tool": {
      "type": "string",
      "minLength": 1,
      "description": "Display name of the CLI under test."
    },
    "command": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 },
      "description": "Invocation prefix. A first element containing a slash resolves against the profile's directory; a bare name resolves through PATH."
    },
    "timeout_seconds": {
      "type": "number",
      "exclusiveMinimum": 0,
      "maximum": 120,
      "description": "Per-run limit. A run that exceeds it is killed and counts as a hang."
    },
    "manifest": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Arguments after the prefix that print the manifest. Omit when the CLI has none; manifest_valid is then skipped."
    },
    "probes": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/definitions/Probe" },
      "description": "Invocations to run. Include at least one read probe; add invalid and destructive probes to enable those checks."
    }
  },
  "definitions": {
    "Probe": {
      "type": "object",
      "required": ["name", "argv", "kind"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Unique label shown in evidence."
        },
        "argv": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Arguments after the command prefix. Never include confirmation flags such as --yes."
        },
        "kind": {
          "type": "string",
          "enum": ["read", "destructive", "invalid"],
          "description": "read: side-effect free. destructive: expected to refuse without confirmation. invalid: expected to exit 2."
        },
        "dry_run_flag": {
          "type": "string",
          "pattern": "^--?[a-z][a-z0-9-]*$",
          "description": "Flag appended to preview a destructive probe. Required when kind is destructive."
        }
      },
      "if": { "properties": { "kind": { "const": "destructive" } } },
      "then": { "required": ["dry_run_flag"] }
    }
  }
}
